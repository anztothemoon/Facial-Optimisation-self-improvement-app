# Env files (secrets) — never commit
backend/.env
frontend/.env
.env
.env.*
!.env.example
!**/.env.example

# Firebase keys often accidentally dropped in repo
**/*firebase-adminsdk*.json

node_modules/
